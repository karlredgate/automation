<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.redgates.base</groupId>
    <artifactId>appsec-diagnostics-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>
  <packaging>war</packaging>
  <artifactId>redgates-base</artifactId>
  <name>Redgates Base WAR</name>
  <description></description>
  <properties>
    <!-- Used by formatter-maven-plugin to locate eclipse-java-google-style.xml -->
    <main.basedir>${project.parent.basedir}</main.basedir>
  </properties>

  <dependencies>
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-test</artifactId>
        <scope>test</scope>
        <exclusions>
          <exclusion>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
  </dependencies>
  <build>
  </build>
  <filters>
    <filter>
    </filter>
  </filters>
  <plugins>
    <plugin>
    </plugin>
  </plugins>
  <profiles>
    <profile>
    </profile>
  </profiles>

</project>
