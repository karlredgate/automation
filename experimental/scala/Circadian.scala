
package com.akamai.csi.tuning.circadian

import java.io.InputStream

object Circadian {
  val MIN_IN_MS = 60 * 1000;

  def getFreqData( trafficMetricValues : Seq[Double] ) : (Array[Double],Array[Double]) = {
  }
}
